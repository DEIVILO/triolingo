type Props = {
    activeCourse: any;            //TODO: REPLACE WITH DATABASE COURSE
    hearts: number;
    points: number; 
    hasActiveSubscription: boolean;
}

export const UserProgress = ({}: Props) => {
    return (
        <div>
            User Progress
        </div>
    );
}
